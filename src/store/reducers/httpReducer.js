import * as actionTypes from '../actions/actionTypes';

const initialState = {};

const httpReducer = (state = initialState, actions) => {
  switch (actions.type) {
  }
  return state;
};

export default httpReducer;
